pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        kotlin("jvm") version "2.4.0"
        kotlin("plugin.serialization") version "2.3.20"
    }
}

rootProject.name = "syllabreak-kotlin"
